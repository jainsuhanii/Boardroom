import React from 'react'

const StepAvatar = ({onNext}) => {
  return ( 
    <> 
    <div>Avtar component</div>
    <button onClick={onNext}>Next</button>
    </>
    )
  };

export default StepAvatar;
